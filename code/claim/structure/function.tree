# Claims about a function: that it is well-defined (one output per input), total (defined everywhere), injective
# (distinct inputs give distinct outputs), surjective (every output is hit), or bijective (both). A function is itself a
# set of input-output pairs; these claims are the properties a proof about a map discharges.

# A unary function takes one element of a set and returns one.
# It is the shape of conjugation, negation, and the norm.
structure unary-function
  head type
  link domain, like set, have type
  link codomain, like set, have type
  link map, like type

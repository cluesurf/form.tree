load @/code/structure/set
  find set

note <A binary function takes two elements of a set and returns one. It is the shape of every algebraic operation, addition, multiplication, the knit.>

form binary-function
  link domain, like set
  link codomain, like set

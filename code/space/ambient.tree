load @term/form/code/structure/space/vector
  find vector-space

# An ambient space: the larger space a shape is considered inside of, the backdrop against which a curve, surface, or
# subspace is embedded. The same circle is one thing intrinsically and another as a subset of the plane it sits in. The
# form carries the surrounding vector space.
form ambient-space
  link space, like vector-space

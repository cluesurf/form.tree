# The properties of a metric: non-negativity (no distance is below zero), identity of indiscernibles (distance is zero
# only between a point and itself), symmetry (distance is the same both ways), and the triangle inequality (a detour is
# never shorter than the direct path). These four laws are what make a distance function a metric; the metric-space form
# is in the space folder.

# Set operations as decidable checks: whether an element is a member, whether one set contains another, whether two sets
# are equal or disjoint. These reduce to the membership predicate. This note records the set checks; the set form is in
# the structure folder.
